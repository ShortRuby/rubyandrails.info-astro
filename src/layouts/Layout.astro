---
import Layout from "./BaseLayout.astro";

const { title, pageTitle, seoDescription, seoKeywords } = Astro.props;
---

<Layout title={title} seoDescription={seoDescription} seoKeywords={seoKeywords}>
  <slot />
</Layout>
